node{
stage('SCM Checkout'){
git branch: 'main', url: 'https://github.com/yuvarakshana-sukumar/EMS_private.git'
}
    stage('Build') {
        def gradleHome = tool name: 'GRADLE_HOME', type: 'gradle'
        sh "${gradleHome} build"
    }
}
